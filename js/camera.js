// Camera is handled via <input type="file" capture="environment"> in index.html.
// No getUserMedia needed — avoids repeated permission prompts on iOS/Android.
// This module is kept for potential future use (e.g. desktop live viewfinder).
